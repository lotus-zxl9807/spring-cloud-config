spring:
  profiles:
    active:
     - dev

---
server:
  port: 7001

spring:
    profiles: dev #开发环境
    application:
       name: springcloud-config-eureka-client

eureka:
  instance:
    hostname: eureka7001.com
  # hostname: localhost 单机版配置
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
     # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ 单机版配置

---
server:
  port: 7001

spring:
    profiles: test #开发环境
    application:
       name: springcloud-config-eureka-client

eureka:
  instance:
    hostname: eureka7001.com
  # hostname: localhost 单机版配置
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
     # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ 单机版配置
